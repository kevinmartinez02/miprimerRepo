name: Run unit tests

on:
  push:
    branches: [main]
    paths:
      - 'week-5/**'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'week-5/package-lock.json'

      - name: Install dependencies
        working-directory: week-5
        run: npm ci

      - name: Run tests
        working-directory: week-5
        run: npm test -- test/brackets.test.ts test/isMultiple.test.ts
